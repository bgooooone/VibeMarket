<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.category.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.shopping.category.entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="sort" property="sort"/>
        <result column="icon" property="icon"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM category WHERE status = 1 ORDER BY sort ASC, id ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM category WHERE parent_id = #{parentId} AND status = 1 ORDER BY sort ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

</mapper>

