<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.cart.mapper.CartMapper">

    <resultMap id="BaseResultMap" type="com.shopping.cart.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="selected" property="selected"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <select id="selectByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectSelectedByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND selected = 1 ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, product_id, quantity, selected)
        VALUES (#{userId}, #{productId}, #{quantity}, #{selected})
    </insert>

    <update id="updateById">
        UPDATE cart
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="selected != null">selected = #{selected},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

    <delete id="deleteSelectedByUserId">
        DELETE FROM cart WHERE user_id = #{userId} AND selected = 1
    </delete>

</mapper>

